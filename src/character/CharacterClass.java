package character;
/* 
* This interface defines the behavior of character classes
* and their abilities within the game's combat system.
*/
public interface CharacterClass {
    int useClassAbility(Character character, Character target);

}
